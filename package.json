{
  "name": "busylight-cli",
  "version": "1.2.0",
  "description": "CLI for kuando busylight",
  "main": "index.js",
  "repository": {    
    "type": "git",
    "url": "git+https://github.com/jamesrichford/busylight-cli.git"
  },
  "bin": {
    "busylight": "./dist/busylight-cli.js"
  },
  "scripts": {
    "build": "rimraf dist && tsc",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "James Richford",
  "license": "MIT",
  "dependencies": {
    "busylight": "^0.3.1"
  },
  "devDependencies": {
    "@types/node": "^8.0.25",
    "rimraf": "^2.6.1",
    "typescript": "^2.4.2"
  }
}
